apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kelpie.fullname" . }}-config
  labels:
    {{- include "kelpie.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
