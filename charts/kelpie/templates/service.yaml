apiVersion: v1
kind: Service
metadata:
  name: {{ include "kelpie.fullname" . }}-web
  labels:
    {{- include "kelpie.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kelpie.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: web
